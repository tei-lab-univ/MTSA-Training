// Parking = EMPTY,
// EMPTY = (in -> FULL),
// FULL = (request -> out -> EMPTY).

range NUM_OF_SLOT = 1..2
PARK = EMPTY,
EMPTY = (in -> FULL),
FULL = (request -> out -> EMPTY).
||PARKS = (p[NUM_OF_SLOT] : PARK).

